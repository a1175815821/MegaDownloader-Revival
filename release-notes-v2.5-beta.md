# v2.5 beta

匿名下载 MEGA 配额专项：配额可预期——自动暂停、诚实倒计时、到点自动恢复。

## ✨ 配额全局熔断 + 倒计时横幅

- 509 / -17 归一识别（状态码判定，不做 body 文本匹配），覆盖文件信息与文件夹读取两个落点
- 递进等待：60 分钟 → 2 小时 → 6 小时封顶；熔断期间不开新任务、不做新校验、分块不空转重试
- 横幅措辞诚实：`MEGA 配额已用尽（未给出确切恢复时间）。已自动暂停，将在 X 小时 Y 分后自动重试` ＋ **[立即重试]** 手动出口（换 IP / 代理后可提前解除）
- 状态栏倒计时 ＋ 进入/解除托盘气泡各一次

## ✨ 自愈默认开 + 永久失败排除 + 批量操作

- `ResetearErrores` 缺省开（15 分钟），存量配置一次性迁移，新装直接开
- `-9 / -11 / -14 / -16` 永久失败不参与自愈；右键新增「重试全部失败」「移除全部失败」（可选删 `.part`）

## ✨ 大文件夹读取进度

- 解析搬出 UI 线程，进度窗实时计数，可取消；在线观看同链路

## 🌐 语言新增

`en-US` / `zh-CN` 新增 11 个键（配额 ×5、批量 ×4、文件夹 ×2），其他语言经 en-US 回退。

---

**构建**：.NET Framework 4.8 · x86 · Win7 SP1+
**版本**：Assembly `2.5.0.0` / InternalConfig `2.5 beta`
**已知限制**：配额倒计时为启发式估算非精确值；配额状态不跨重启持久化；更新检查在 beta 期间不提示（本地 2.5 > feed 2.4.7）。
