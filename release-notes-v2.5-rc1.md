# v2.5 RC1（测试版）

> ⚠️ 这是测试预发布，不是正式版：仅用于验证本轮修复，旁边的 `Latest` 正式版不受影响，软件内也不会提示更新。绿色免安装，解压即用；首次运行前建议备份 `%LOCALAPPDATA%\MegaDownloader\Config\Configuration.xml`。

匿名下载 MEGA 配额专项：配额可预期——自动暂停、诚实倒计时、到点自动恢复。

## ✨ 配额全局熔断 + 倒计时横幅

- 509 / -17 归一识别（状态码判定，不做 body 文本匹配），覆盖文件信息与文件夹读取两个落点
- 递进等待：60 分钟 → 2 小时 → 6 小时封顶（24 小时无命中才回落），熔断期间不开新任务、不做新校验、分块不空转重试
- 横幅措辞诚实：`MEGA 配额已用尽……将在 X 小时 Y 分后自动重试` ＋ **[立即重试]** 手动出口（换 IP / 代理后可提前解除）
- 状态栏倒计时（120 秒内读秒）＋ 进入/解除托盘气泡各一次

## ✨ 自愈默认开 + 永久失败排除 + 批量操作

- 失败自愈缺省开（15 分钟），存量配置一次性迁移；**配额到期唤醒不再依赖该开关**
- `-9 / -11 / -14 / -16` 永久失败不参与自愈；右键新增「重试全部失败」「移除全部失败」（可选删 `.part`）

## ✨ 大文件夹读取进度

- 解析搬出 UI 线程，进度窗实时计数；取消是真取消（停掉后台解析，不再空耗 API）；进度条跟随深色主题

## 🐛 本轮修复（测试重点）

- 下载列表列宽防损坏：表头拖拽限宽、启动时坏状态（被压成 0 / 上千像素）自动恢复默认；右键新增「恢复默认列宽」；左侧导航计数改为按包统计（此前包和文件重复累加）
- 进度条重绘：浅色整条发灰消除，小进度保底可见
- 配额熔断期内超时正确标配额，「立即重试」可捞回；120 秒为总时长看门狗（非空闲超时）
- 重启后「查看错误」不再空白；错误/永久/配额标记落盘，重启不丢
- es-ES 补 11 个键；另有十余项 UI 可感知修复，详见 `docs/CHANGELOG.md`

## 📦 包内容

`MegaDownloader.exe` ＋ 12 个 DLL ＋ `MegaDownloader.exe.config`，须放同一目录（新增的 5 个 `System.*.dll` 为新版构建工具链所需，缺失启动即崩）。

---

**构建**：.NET Framework 4.8 · x86 · Win7 SP1+
**版本**：Assembly `2.5.0.0` / InternalConfig `2.5 RC1`
**已知限制**：配额倒计时为启发式估算；配额计时本身不跨重启（失败标记已持久化）；顶层系统菜单深色下仍浅色；本测试版不进更新通道。
