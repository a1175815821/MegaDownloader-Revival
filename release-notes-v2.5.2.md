# v2.5.2

> 免安装，解压即用；从 2.5.x 升级前建议备份 `%LOCALAPPDATA%\MegaDownloader\Config\Configuration.xml`。注意：设置与下载队列保存在该目录，不在程序目录下，整文件夹拷走不会带走配置。

## 本次变更

一句话：**下完会叫你，报错说人话，大文件夹不卡，导入不转圈**。

- **完成通知 / 托盘气泡**：任务包下完弹一条，点击回到主窗口；失败也弹，且直接告诉你怎么办（见下）。存量红字重启不补弹，批量失败自动合并，60 秒刷屏只记日志
- **错误说人话 / 失败建议**：配额用尽、链接失效、连接重试三档翻译，不再是天书错误码
- **大文件夹不卡 / 列表刷新优化**：几百个文件的包，界面刷新不再全局加锁硬扫（短锁快照 + 批量重绘），滚动和点击回到正常
- **导入不再转圈 / 解析可取消、校验并行**：文件夹解析中途可随时取消（此前取消键几分钟没反应）；每个文件的验信息从串行改 4 并行，大文件夹导入耗时直接除以 4；进度窗显示已用秒数
- **口令加固 / 代理与流媒体口令改 DPAPI 加密存储**：与 MEGA 账号同等待遇。老配置的明文口令首次保存时自动迁移；**如果是从备份恢复到其他机器/账户**，解密失败会在打开设置时提示重填（不再静默丢失）
- **依赖升级 / 十年欠账一次结清**：SharpCompress 0.10→0.49（先补 13 项解压回归用例再升）、BouncyCastle 1.7→2.6.2、Newtonsoft.Json 4.5→13（顺手堵掉一个已公开的 JSON 解析 DoS 漏洞）。随包 DLL 从 12 个增加到 15 个，单文件版照常双击即用
- **落盘更快 / fsync 降频不断电丢数**：每次写都 fsync 改为 8MB/5 秒节流，另记已同步边界，配置文件只持久化已落盘进度——断电/强杀重启后最多重下窗口内几 MB，永不出现空洞损坏
- **锁换轻量 / Mutex → SyncLock**：全仓 100+ 处内核锁换进程内轻量锁，修掉两处历史锁泄漏
- **文档门面 / README 实拍截图**：浅色/深色两张主界面实拍；CHANGELOG 收敛为简体中文单文件；CONTRIBUTING 只留英文与简体中文

详见 `docs/CHANGELOG.md`（简体中文）。

## 📦 包内容

`MegaDownloader.exe` ＋ 15 个 DLL ＋ `MegaDownloader.exe.config`，须放同一目录。单文件版约 11.6MB（内嵌 DLL 随依赖升级变大，zip 包会再压小）。

## 已知限制

公开链接本质无端到端校验（仅长度）；配额倒计时为启发式估算；7z 解压仅总量进度（无逐字节）；RAR 分卷/加密/固实包建议先用小文件试解；顶层系统菜单深色下仍浅色。

---

**构建**：.NET Framework 4.8 · x86 · Win7 SP1+
**版本**：Assembly `2.5.2.0` / InternalConfig `2.5.2`
