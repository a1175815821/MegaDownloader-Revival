# v2.5 RC2（测试版）

> ⚠️ 这是测试预发布，不是正式版：仅用于验证本轮修复，旁边的 `Latest` 正式版不受影响，软件内也不会提示更新。绿色免安装，解压即用；首次运行前建议备份 `%LOCALAPPDATA%\MegaDownloader\Config\Configuration.xml`。

生产级破坏性审计后的完整修复：发布阻塞 5 项 + 可靠性 4 项 + 体验与纵深。核心主题：**消灭静默失败与高频卡顿**。

## 🐛 发布阻塞修复（必测）

- Web/局域网推送：文件夹链接正常展开（此前变单个坏任务）；包名强制净化，子目录结构保留
- 文件夹下载：单个坏节点不再让整包“零文件报成功”——部分跳过记日志警告，全部失败按失败处理
- 0 字节空文件：正常落盘+校验+完成（此前永远失败、自愈空转）
- 验证中点暂停/停止/关机：不再复活下载
- 好坏 ELC 混粘：坏的只跳过自己，好的照常入库

## 🐛 可靠性修复

- GB 大文件 100% 那一下不再冻结 UI（MD5 移出全局锁）
- 设过 Web 密码的用户：`Configuration.xml` 不再每 5 秒重写
- 有验证/解压/MD5 进行中时关机：等它们结束再存盘，不再撕裂队列
- 看门狗超时与撞线完成竞态：完好文件不再被钉成错误

## 🐛 体验与纵深

- 粘贴数百链接不再卡死（正则单例化）；论坛转义链接（`%23/%3D`）不再永久“无法解密”
- 更新只走 https；公开链接无端到端校验记警告留痕；Stegano 远端图片 64MB 上限

## 📦 包内容

`MegaDownloader.exe` ＋ 12 个 DLL ＋ `MegaDownloader.exe.config`，须放同一目录。

---

**构建**：.NET Framework 4.8 · x86 · Win7 SP1+
**版本**：Assembly `2.5.0.0` / InternalConfig `2.5 RC2`
**已知限制**：公开链接本质无端到端校验（仅长度）；配额倒计时为启发式估算；顶层系统菜单深色下仍浅色；本测试版不进更新通道。
